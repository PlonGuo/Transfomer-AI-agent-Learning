# AI工程化快速实战路线（4-6周）

> 专注工程落地能力 - RAG + Fine-tuning + Agent编排
> 
> 为Jason定制 - 快速掌握AI应用开发
> 
> 最后更新: 2025-01-05

---

## 📋 目录

1. [核心思路](#核心思路)
2. [环境准备](#环境准备)
3. [Week 1-2: RAG系统实战](#week-1-2-rag系统实战)
4. [Week 3: Fine-tuning实战](#week-3-fine-tuning实战)
5. [Week 4-5: Agent工作流编排](#week-4-5-agent工作流编排)
6. [Week 6: 项目整合](#week-6-项目整合)
7. [快速参考](#快速参考)

---

## 🎯 核心思路

```
跳过理论深挖 → 直接实战
─────────────────────────────
❌ 不学：手撸Transformer
❌ 不学：PyTorch底层
❌ 不学：数学推导

✅ 只学：工程化落地
✅ 重点：RAG系统
✅ 重点：Fine-tuning实战
✅ 重点：Agent工作流编排
✅ 目标：2个production-ready项目
```

### 学习原则

1. **复制粘贴优先** - 用现成的代码模板快速迭代
2. **够用就行** - 不深究原理，能用就行
3. **每周一个项目** - 立刻能demo的成果
4. **遇到问题立刻问** - 不要卡超过1小时

---

## 🛠️ 环境准备

### 安装依赖（15分钟）

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Mac/Linux
# 或 venv\Scripts\activate  # Windows

# 2. 安装核心包
pip install langchain langchain-openai langgraph

# 3. 安装辅助包
pip install chromadb streamlit fastapi uvicorn
pip install yfinance pandas numpy matplotlib

# 4. 安装Fine-tuning相关（Week 3需要）
pip install transformers datasets accelerate

# 5. 验证安装
python -c "
from langchain_openai import ChatOpenAI
from langgraph.graph import StateGraph
print('✅ 安装成功!')
"
```

### API Keys准备

```bash
# 必需（二选一）
export OPENAI_API_KEY="your-openai-key"
# 或
export ANTHROPIC_API_KEY="your-claude-key"

# 可选
export NEWS_API_KEY="your-news-api-key"
```

### 创建项目结构

```bash
mkdir ai-stock-research
cd ai-stock-research

# 项目结构
ai-stock-research/
├── week1-rag/           # RAG系统
├── week3-finetuning/    # Fine-tuning
├── week4-agent/         # Agent编排
├── final-project/       # 最终项目
└── README.md
```

---

## 📚 Week 1-2: RAG系统实战

**目标**: 快速构建能用的RAG系统

### Day 1-2: LangChain RAG速成（4小时）

#### 资源1: LangChain RAG官方教程
- **链接**: https://python.langchain.com/docs/tutorials/rag/
- **时长**: 2小时
- **重点**: Document loading, Splitting, Embedding, Retrieval

#### 第一个RAG原型（30分钟跑通）

```python
# File: simple_rag.py
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_community.vectorstores import Chroma
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.chains import RetrievalQA
from langchain_community.document_loaders import TextLoader

def create_rag_system():
    # 1. 创建测试文档
    with open("trading_notes.txt", "w") as f:
        f.write("""
        NVDA Trading Strategy:
        - Entry: When RSI < 30
        - Exit: When RSI > 70
        - Stop loss: 5% below entry
        
        TSLA Analysis:
        - Strong momentum in Q3 2024
        - Revenue beat expectations
        """)
    
    # 2. 加载文档
    loader = TextLoader("trading_notes.txt")
    documents = loader.load()
    
    # 3. 切分
    splitter = RecursiveCharacterTextSplitter(chunk_size=200)
    chunks = splitter.split_documents(documents)
    
    # 4. 创建向量库
    vectorstore = Chroma.from_documents(
        documents=chunks,
        embedding=OpenAIEmbeddings()
    )
    
    # 5. 创建QA链
    qa = RetrievalQA.from_chain_type(
        llm=ChatOpenAI(),
        retriever=vectorstore.as_retriever()
    )
    
    return qa

# 测试
if __name__ == "__main__":
    qa = create_rag_system()
    
    question = "What's my NVDA trading strategy?"
    answer = qa.invoke(question)
    print(f"Q: {question}")
    print(f"A: {answer['result']}")
```

**运行**:
```bash
export OPENAI_API_KEY="your-key"
python simple_rag.py
```

#### Day 1-2任务清单
- [ ] 跑通上面的代码（30分钟）
- [ ] 用你的量化交易笔记替换测试文档
- [ ] 测试3-5个不同的问题
- [ ] 观察回答质量

### Day 3-5: RAG优化实战（6小时）

#### 优化点1: 更好的文档处理

```python
# 支持PDF文档
from langchain_community.document_loaders import PyPDFLoader, DirectoryLoader

# 方法1: 单个PDF
loader = PyPDFLoader("trading_strategy.pdf")
documents = loader.load()

# 方法2: 整个文件夹
loader = DirectoryLoader(
    "./trading_notes/",
    glob="**/*.pdf",
    loader_cls=PyPDFLoader
)
documents = loader.load()

print(f"Loaded {len(documents)} documents")
```

#### 优化点2: 混合检索（Hybrid Search）

```python
# 结合语义搜索和关键词搜索
from langchain.retrievers import EnsembleRetriever
from langchain_community.retrievers import BM25Retriever

# 语义检索
vector_retriever = vectorstore.as_retriever(search_kwargs={"k": 3})

# 关键词检索
bm25_retriever = BM25Retriever.from_documents(documents)
bm25_retriever.k = 3

# 混合
ensemble_retriever = EnsembleRetriever(
    retrievers=[vector_retriever, bm25_retriever],
    weights=[0.7, 0.3]  # 70%语义，30%关键词
)

# 使用混合检索
qa = RetrievalQA.from_chain_type(
    llm=ChatOpenAI(),
    retriever=ensemble_retriever
)
```

#### 优化点3: Re-ranking提升准确度

```python
from langchain.retrievers import ContextualCompressionRetriever
from langchain.retrievers.document_compressors import LLMChainExtractor

# 用LLM重新排序检索结果
compressor = LLMChainExtractor.from_llm(llm)

compression_retriever = ContextualCompressionRetriever(
    base_compressor=compressor,
    base_retriever=vector_retriever
)
```

### Day 6-7: 添加Web界面（4小时）

#### Streamlit界面（1小时搭建）

```python
# File: app.py
import streamlit as st
from simple_rag import create_rag_system

st.set_page_config(page_title="Stock Research Assistant", page_icon="📈")

st.title("📈 Stock Research Assistant")

# 侧边栏
with st.sidebar:
    st.header("配置")
    uploaded_file = st.file_uploader(
        "上传交易笔记 (PDF/TXT)",
        type=['pdf', 'txt']
    )
    
    if uploaded_file:
        st.success("文档已上传!")

# 主界面
st.markdown("### 💬 问答")

# 初始化session state
if "messages" not in st.session_state:
    st.session_state.messages = []
    st.session_state.qa = create_rag_system()

# 显示聊天历史
for message in st.session_state.messages:
    with st.chat_message(message["role"]):
        st.markdown(message["content"])

# 用户输入
if prompt := st.chat_input("询问你的交易策略..."):
    # 显示用户消息
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.markdown(prompt)
    
    # 获取AI回答
    with st.chat_message("assistant"):
        with st.spinner("思考中..."):
            response = st.session_state.qa.invoke(prompt)
            answer = response['result']
            st.markdown(answer)
    
    # 保存AI回答
    st.session_state.messages.append({"role": "assistant", "content": answer})
```

**运行**:
```bash
streamlit run app.py
```

### Week 1-2交付物

**完成标准**:
- ✅ RAG系统能正确检索文档
- ✅ 支持PDF和TXT文件
- ✅ 有简洁的Web界面
- ✅ 回答准确且有来源
- ✅ GitHub repo + README
- ✅ 5分钟demo视频

**验收问题**:
1. "RAG的三个核心步骤是什么？"
2. "展示你的RAG系统查询效果"
3. "如何提升检索准确度？"

---

## 🤖 Week 3: Fine-tuning实战

**目标**: 快速掌握模型微调

### Day 1-2: HuggingFace速成（4小时）

#### 资源1: HuggingFace Quick Tour
- **链接**: https://huggingface.co/docs/transformers/quicktour
- **时长**: 1小时
- **重点**: 加载模型、使用pipeline

#### 资源2: Fine-tuning教程
- **链接**: https://huggingface.co/docs/transformers/training
- **时长**: 2小时
- **重点**: Trainer API、训练配置

#### 第一个Fine-tuning例子（1小时跑通）

```python
# File: finetune_sentiment.py
from transformers import (
    AutoTokenizer,
    AutoModelForSequenceClassification,
    Trainer,
    TrainingArguments
)
from datasets import load_dataset

# 1. 加载预训练模型
model_name = "distilbert-base-uncased"
model = AutoModelForSequenceClassification.from_pretrained(
    model_name,
    num_labels=3  # 正面、负面、中性
)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# 2. 加载数据集（金融情绪数据）
dataset = load_dataset("financial_phrasebank", "sentences_allagree")

# 3. 预处理
def tokenize_function(examples):
    return tokenizer(
        examples["sentence"],
        padding="max_length",
        truncation=True
    )

tokenized_datasets = dataset.map(tokenize_function, batched=True)

# 4. 训练配置
training_args = TrainingArguments(
    output_dir="./results",
    num_train_epochs=3,
    per_device_train_batch_size=16,
    per_device_eval_batch_size=64,
    warmup_steps=500,
    weight_decay=0.01,
    logging_dir="./logs",
    logging_steps=100,
    evaluation_strategy="steps",
    eval_steps=500,
)

# 5. 创建Trainer
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_datasets["train"],
    eval_dataset=tokenized_datasets["validation"],
)

# 6. 开始训练
trainer.train()

# 7. 保存模型
model.save_pretrained("./financial-sentiment-model")
tokenizer.save_pretrained("./financial-sentiment-model")

print("✅ Fine-tuning完成!")
```

### Day 3-5: 实战Fine-tuning项目（8小时）

#### 项目: 金融情绪分析模型

**数据准备**:
```python
# 方法1: 使用现成数据集
from datasets import load_dataset

dataset = load_dataset("financial_phrasebank", "sentences_allagree")

# 方法2: 自己标注数据
import pandas as pd

data = {
    'text': [
        "NVIDIA reports record revenue",
        "Stock market crashes amid recession fears",
        "Company announces layoffs"
    ],
    'label': [2, 0, 0]  # 0=负面, 1=中性, 2=正面
}

df = pd.DataFrame(data)
df.to_csv("my_financial_sentiment.csv", index=False)
```

**训练脚本**（完整版）:
```python
# File: train_sentiment.py
from transformers import (
    AutoTokenizer,
    AutoModelForSequenceClassification,
    Trainer,
    TrainingArguments,
    EarlyStoppingCallback
)
from datasets import load_dataset
import numpy as np
from sklearn.metrics import accuracy_score, f1_score

# 加载数据
dataset = load_dataset("financial_phrasebank", "sentences_allagree")

# 加载模型
model_name = "distilbert-base-uncased"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForSequenceClassification.from_pretrained(
    model_name,
    num_labels=3
)

# 数据预处理
def preprocess(examples):
    return tokenizer(
        examples["sentence"],
        truncation=True,
        padding="max_length",
        max_length=128
    )

tokenized = dataset.map(preprocess, batched=True)

# 评估指标
def compute_metrics(eval_pred):
    predictions, labels = eval_pred
    predictions = np.argmax(predictions, axis=1)
    
    return {
        'accuracy': accuracy_score(labels, predictions),
        'f1': f1_score(labels, predictions, average='weighted')
    }

# 训练配置
training_args = TrainingArguments(
    output_dir="./sentiment-model",
    num_train_epochs=3,
    per_device_train_batch_size=16,
    per_device_eval_batch_size=32,
    warmup_steps=500,
    weight_decay=0.01,
    logging_dir="./logs",
    logging_steps=100,
    evaluation_strategy="steps",
    eval_steps=500,
    save_strategy="steps",
    save_steps=500,
    load_best_model_at_end=True,
    metric_for_best_model="accuracy",
)

# 训练
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized["train"],
    eval_dataset=tokenized["validation"],
    compute_metrics=compute_metrics,
    callbacks=[EarlyStoppingCallback(early_stopping_patience=3)]
)

# 开始训练
print("🚀 开始训练...")
trainer.train()

# 评估
print("\n📊 评估结果:")
eval_results = trainer.evaluate()
print(eval_results)

# 保存
trainer.save_model("./final-sentiment-model")
print("\n✅ 模型已保存到 ./final-sentiment-model")
```

#### 使用Fine-tuned模型

```python
# File: use_model.py
from transformers import pipeline

# 加载模型
classifier = pipeline(
    "sentiment-analysis",
    model="./final-sentiment-model"
)

# 测试
texts = [
    "NVIDIA stock surges on strong earnings",
    "Market crashes amid economic uncertainty",
    "Company reports stable quarterly results"
]

results = classifier(texts)
for text, result in zip(texts, results):
    print(f"Text: {text}")
    print(f"Sentiment: {result['label']}, Confidence: {result['score']:.2f}\n")
```

### Day 6-7: 部署为API（4小时）

```python
# File: api.py
from fastapi import FastAPI
from pydantic import BaseModel
from transformers import pipeline

app = FastAPI()

# 加载模型
classifier = pipeline("sentiment-analysis", model="./final-sentiment-model")

class TextRequest(BaseModel):
    text: str

@app.post("/analyze")
async def analyze_sentiment(request: TextRequest):
    result = classifier(request.text)[0]
    return {
        "text": request.text,
        "sentiment": result['label'],
        "confidence": result['score']
    }

@app.get("/")
async def root():
    return {"message": "Financial Sentiment API"}

# 运行: uvicorn api:app --reload
```

### Week 3交付物

**完成标准**:
- ✅ Fine-tuned金融情绪分析模型
- ✅ 准确率>80%
- ✅ 部署为API
- ✅ 模型上传到HuggingFace Hub（可选）
- ✅ 测试报告

**验收问题**:
1. "展示你Fine-tuned的模型效果"
2. "训练了多少epoch？准确率如何？"
3. "如何在HuggingFace上Fine-tune模型？"

---

## 🤖 Week 4-5: Agent工作流编排

**目标**: 用LangGraph构建Multi-agent系统

### Day 1: LangChain前置知识（3小时）

#### 必学概念（仅此而已）

**1. LLM调用（15分钟）**:
```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(model="gpt-4", temperature=0)
response = llm.invoke("Hello!")
print(response.content)
```

**2. Tool定义（30分钟）**:
```python
from langchain.tools import tool

@tool
def get_stock_price(ticker: str) -> str:
    """Get current stock price for a ticker."""
    import yfinance as yf
    stock = yf.Ticker(ticker)
    price = stock.info.get('currentPrice', 'N/A')
    return f"{ticker}: ${price}"

@tool
def calculate_rsi(ticker: str) -> str:
    """Calculate RSI indicator."""
    # 简化实现
    return f"RSI for {ticker}: 45.5"

# 测试
result = get_stock_price.invoke({"ticker": "AAPL"})
print(result)
```

**3. Simple Agent（1小时）**:
```python
from langchain.agents import create_tool_calling_agent, AgentExecutor
from langchain.prompts import ChatPromptTemplate

tools = [get_stock_price, calculate_rsi]

prompt = ChatPromptTemplate.from_messages([
    ("system", "You are a stock analysis assistant."),
    ("human", "{input}"),
    ("placeholder", "{agent_scratchpad}"),
])

agent = create_tool_calling_agent(llm, tools, prompt)
executor = AgentExecutor(agent=agent, tools=tools, verbose=True)

# 测试
result = executor.invoke({"input": "What's AAPL price and RSI?"})
print(result["output"])
```

### Day 2-3: LangGraph核心（6小时）

#### 资源: LangGraph官方教程
- **链接**: https://langchain-ai.github.io/langgraph/tutorials/introduction/
- **时长**: 3小时
- **重点**: StateGraph, Node, Edge, Conditional routing

#### LangGraph核心概念

**概念1: State（状态）**:
```python
from typing import TypedDict

class AgentState(TypedDict):
    messages: list      # 对话历史
    ticker: str         # 股票代码
    data: dict          # 数据
    analysis: str       # 分析结果
    next_step: str      # 下一步动作
```

**概念2: Node（节点）= 函数**:
```python
def research_node(state: AgentState) -> AgentState:
    """获取股票数据"""
    ticker = state["ticker"]
    # 调用API获取数据
    data = fetch_stock_data(ticker)
    return {"data": data}

def analysis_node(state: AgentState) -> AgentState:
    """分析数据"""
    analysis = analyze_stock(state["data"])
    return {"analysis": analysis}
```

**概念3: Graph（图）= 工作流**:
```python
from langgraph.graph import StateGraph, END

# 创建工作流
workflow = StateGraph(AgentState)

# 添加节点
workflow.add_node("research", research_node)
workflow.add_node("analysis", analysis_node)

# 设置入口
workflow.set_entry_point("research")

# 添加边（定义流程）
workflow.add_edge("research", "analysis")
workflow.add_edge("analysis", END)

# 编译
app = workflow.compile()

# 运行
result = app.invoke({
    "ticker": "NVDA",
    "messages": []
})
```

#### 第一个完整LangGraph Agent（2小时）

```python
# File: langgraph_agent.py
from langgraph.graph import StateGraph, END
from typing import TypedDict, Literal
from langchain_openai import ChatOpenAI
from langchain.tools import tool
import yfinance as yf

# 1. 定义状态
class TradingState(TypedDict):
    ticker: str
    price: float
    rsi: float
    recommendation: str
    messages: list

# 2. 定义工具
@tool
def fetch_price(ticker: str) -> float:
    """Fetch current stock price."""
    stock = yf.Ticker(ticker)
    return stock.info.get('currentPrice', 0)

@tool
def calculate_rsi(ticker: str) -> float:
    """Calculate RSI indicator."""
    stock = yf.Ticker(ticker)
    hist = stock.history(period="3mo")
    
    # 简化RSI计算
    delta = hist['Close'].diff()
    gain = delta.where(delta > 0, 0).rolling(14).mean()
    loss = -delta.where(delta < 0, 0).rolling(14).mean()
    rs = gain / loss
    rsi = 100 - (100 / (1 + rs))
    
    return float(rsi.iloc[-1])

# 3. 定义节点
def fetch_data_node(state: TradingState) -> TradingState:
    ticker = state["ticker"]
    price = fetch_price.invoke({"ticker": ticker})
    rsi = calculate_rsi.invoke({"ticker": ticker})
    
    return {
        "price": price,
        "rsi": rsi,
        "messages": state["messages"] + [f"Fetched data for {ticker}"]
    }

def analyze_node(state: TradingState) -> TradingState:
    llm = ChatOpenAI(model="gpt-4", temperature=0)
    
    prompt = f"""
    Analyze {state['ticker']}:
    - Current Price: ${state['price']}
    - RSI: {state['rsi']}
    
    Provide a trading recommendation (BUY/HOLD/SELL) with reasoning.
    """
    
    analysis = llm.invoke(prompt).content
    
    return {
        "recommendation": analysis,
        "messages": state["messages"] + ["Analysis completed"]
    }

# 4. 条件判断
def should_analyze(state: TradingState) -> Literal["analyze", "end"]:
    """决定是否继续分析"""
    if state.get("price", 0) > 0:
        return "analyze"
    return "end"

# 5. 构建工作流
workflow = StateGraph(TradingState)

# 添加节点
workflow.add_node("fetch_data", fetch_data_node)
workflow.add_node("analyze", analyze_node)

# 设置流程
workflow.set_entry_point("fetch_data")
workflow.add_conditional_edges(
    "fetch_data",
    should_analyze,
    {
        "analyze": "analyze",
        "end": END
    }
)
workflow.add_edge("analyze", END)

# 编译
app = workflow.compile()

# 测试
if __name__ == "__main__":
    result = app.invoke({
        "ticker": "NVDA",
        "messages": [],
        "price": 0,
        "rsi": 0,
        "recommendation": ""
    })
    
    print("\n" + "="*60)
    print("分析结果:")
    print("="*60)
    print(f"股票: {result['ticker']}")
    print(f"价格: ${result['price']}")
    print(f"RSI: {result['rsi']:.2f}")
    print(f"\n推荐:\n{result['recommendation']}")
```

### Day 4-7: Multi-Agent系统（10小时）

#### 构建多Agent协作系统

```python
# File: multi_agent.py
from langgraph.graph import StateGraph, END
from typing import TypedDict
from langchain_openai import ChatOpenAI

class MultiAgentState(TypedDict):
    ticker: str
    research_data: dict
    technical_analysis: dict
    fundamental_analysis: dict
    final_report: str
    messages: list

# === Agent 1: Research Agent ===
def research_agent(state: MultiAgentState) -> MultiAgentState:
    """收集数据"""
    ticker = state["ticker"]
    
    # 获取多种数据
    import yfinance as yf
    stock = yf.Ticker(ticker)
    
    research_data = {
        "info": stock.info,
        "history": stock.history(period="1mo"),
        "news": stock.news[:5] if hasattr(stock, 'news') else []
    }
    
    return {
        "research_data": research_data,
        "messages": state["messages"] + ["Research completed"]
    }

# === Agent 2: Technical Analysis Agent ===
def technical_agent(state: MultiAgentState) -> MultiAgentState:
    """技术分析"""
    hist = state["research_data"]["history"]
    
    # 计算指标
    analysis = {
        "sma_20": hist['Close'].rolling(20).mean().iloc[-1],
        "sma_50": hist['Close'].rolling(50).mean().iloc[-1],
        "current_price": hist['Close'].iloc[-1],
        "volume": hist['Volume'].iloc[-1]
    }
    
    return {
        "technical_analysis": analysis,
        "messages": state["messages"] + ["Technical analysis done"]
    }

# === Agent 3: Fundamental Analysis Agent ===
def fundamental_agent(state: MultiAgentState) -> MultiAgentState:
    """基本面分析"""
    info = state["research_data"]["info"]
    
    analysis = {
        "pe_ratio": info.get('forwardPE', 'N/A'),
        "market_cap": info.get('marketCap', 'N/A'),
        "revenue": info.get('totalRevenue', 'N/A'),
        "profit_margin": info.get('profitMargins', 'N/A')
    }
    
    return {
        "fundamental_analysis": analysis,
        "messages": state["messages"] + ["Fundamental analysis done"]
    }

# === Agent 4: Report Agent ===
def report_agent(state: MultiAgentState) -> MultiAgentState:
    """生成综合报告"""
    llm = ChatOpenAI(model="gpt-4")
    
    prompt = f"""
    Generate a comprehensive stock analysis report for {state['ticker']}.
    
    Technical Analysis:
    {state['technical_analysis']}
    
    Fundamental Analysis:
    {state['fundamental_analysis']}
    
    Provide:
    1. Summary
    2. Key insights
    3. Trading recommendation (BUY/HOLD/SELL)
    4. Risk factors
    """
    
    report = llm.invoke(prompt).content
    
    return {
        "final_report": report,
        "messages": state["messages"] + ["Report generated"]
    }

# === 构建Multi-Agent工作流 ===
workflow = StateGraph(MultiAgentState)

# 添加所有agents
workflow.add_node("research", research_agent)
workflow.add_node("technical", technical_agent)
workflow.add_node("fundamental", fundamental_agent)
workflow.add_node("report", report_agent)

# 设置流程（并行分析）
workflow.set_entry_point("research")
workflow.add_edge("research", "technical")
workflow.add_edge("research", "fundamental")
workflow.add_edge("technical", "report")
workflow.add_edge("fundamental", "report")
workflow.add_edge("report", END)

# 编译
app = workflow.compile()

# 测试
if __name__ == "__main__":
    result = app.invoke({
        "ticker": "TSLA",
        "messages": [],
        "research_data": {},
        "technical_analysis": {},
        "fundamental_analysis": {},
        "final_report": ""
    })
    
    print("\n" + "="*60)
    print("多Agent分析报告")
    print("="*60)
    print(result["final_report"])
```

### Week 4-5交付物

**完成标准**:
- ✅ Multi-agent工作流系统
- ✅ 至少3个协作的agents
- ✅ 能自动完成：数据获取→分析→报告生成
- ✅ 工作流可视化（LangGraph自带）
- ✅ GitHub repo

**验收问题**:
1. "展示你的Multi-agent系统运行过程"
2. "各个Agent如何协作？"
3. "如何用LangGraph构建workflow？"

---

## 🎯 Week 6: 项目整合

**最终项目**: AI量化交易研究系统

### 项目架构

```
┌─────────────────────────────────────┐
│         React Frontend              │
│  - 股票搜索界面                      │
│  - Agent工作流可视化                 │
│  - 分析报告展示                      │
│  - RAG问答聊天框                     │
└─────────────────────────────────────┘
              ↓ REST API
┌─────────────────────────────────────┐
│      FastAPI Backend                │
│  - /api/analyze/{ticker}            │
│  - /api/rag/query                   │
│  - /api/sentiment/analyze           │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Multi-Agent System              │
│  ├─ Research Agent                  │
│  │   └─ Tools: yfinance, NewsAPI    │
│  ├─ Technical Analysis Agent        │
│  │   └─ Tools: TA指标计算            │
│  ├─ Fundamental Analysis Agent      │
│  │   └─ Tools: 财务数据              │
│  ├─ RAG Agent                       │
│  │   └─ 查询交易笔记知识库           │
│  └─ Report Agent                    │
│      └─ 生成综合报告                │
└─────────────────────────────────────┘
```

### Day 1-2: 后端API开发（6小时）

```python
# File: main.py
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel
from multi_agent import app as agent_app
from simple_rag import create_rag_system

app = FastAPI(title="AI Stock Research API")

# 初始化
rag_system = create_rag_system()

class AnalyzeRequest(BaseModel):
    ticker: str

class RAGQuery(BaseModel):
    question: str

@app.post("/api/analyze/{ticker}")
async def analyze_stock(ticker: str):
    """Multi-agent股票分析"""
    try:
        result = agent_app.invoke({
            "ticker": ticker,
            "messages": [],
            "research_data": {},
            "technical_analysis": {},
            "fundamental_analysis": {},
            "final_report": ""
        })
        
        return {
            "ticker": ticker,
            "report": result["final_report"],
            "status": "success"
        }
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

@app.post("/api/rag/query")
async def rag_query(query: RAGQuery):
    """RAG知识库查询"""
    try:
        answer = rag_system.invoke(query.question)
        return {
            "question": query.question,
            "answer": answer['result'],
            "status": "success"
        }
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

@app.get("/")
async def root():
    return {
        "message": "AI Stock Research API",
        "endpoints": [
            "/api/analyze/{ticker}",
            "/api/rag/query"
        ]
    }

# 运行: uvicorn main:app --reload
```

### Day 3-5: 前端开发（8小时）

```typescript
// App.tsx
import { useState } from 'react';
import axios from 'axios';

function App() {
  const [ticker, setTicker] = useState('');
  const [report, setReport] = useState('');
  const [loading, setLoading] = useState(false);

  const analyzeStock = async () => {
    setLoading(true);
    try {
      const response = await axios.post(
        `http://localhost:8000/api/analyze/${ticker}`
      );
      setReport(response.data.report);
    } catch (error) {
      console.error(error);
    }
    setLoading(false);
  };

  return (
    <div className="App">
      <h1>📈 AI Stock Research</h1>
      
      <div className="search-section">
        <input
          type="text"
          placeholder="Enter ticker (e.g., NVDA)"
          value={ticker}
          onChange={(e) => setTicker(e.target.value.toUpperCase())}
        />
        <button onClick={analyzeStock} disabled={loading}>
          {loading ? 'Analyzing...' : 'Analyze'}
        </button>
      </div>

      {report && (
        <div className="report-section">
          <h2>Analysis Report</h2>
          <pre>{report}</pre>
        </div>
      )}
    </div>
  );
}

export default App;
```

### Day 6-7: 集成测试和文档（4小时）

#### 项目README模板

```markdown
# AI Stock Research System

AI-powered multi-agent system for stock analysis and research.

## Features

- 🤖 Multi-Agent Architecture
  - Research Agent: Data collection
  - Technical Analysis Agent: TA indicators
  - Fundamental Analysis Agent: Financial metrics
  - Report Agent: Comprehensive reports

- 📚 RAG Knowledge Base
  - Query your trading notes
  - Semantic search
  - Context-aware answers

- 🎨 Web Interface
  - Clean React UI
  - Real-time analysis
  - Interactive chat

## Tech Stack

**Backend**:
- Python + FastAPI
- LangGraph (Agent orchestration)
- LangChain (RAG system)
- OpenAI GPT-4

**Frontend**:
- React + TypeScript
- Axios
- Tailwind CSS

**Data**:
- yfinance (Stock data)
- Chroma (Vector database)

## Quick Start

### Backend
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm start
```

## Demo

[5分钟demo视频链接]

## Architecture

[架构图]

## Future Improvements

- [ ] Add more technical indicators
- [ ] Implement backtesting
- [ ] Add risk management module
- [ ] Deploy to cloud

## Author

Jason - [GitHub](https://github.com/yourusername)
```

### Week 6交付物

**最终项目包含**:
- ✅ 完整的Multi-agent后端
- ✅ React前端界面
- ✅ RAG知识库集成
- ✅ Fine-tuned模型集成（可选）
- ✅ 完整README和文档
- ✅ 5分钟demo视频
- ✅ 部署到本地可访问

**验收问题**:
1. "演示完整的股票分析流程"
2. "讲解你的Multi-agent架构"
3. "遇到了哪些技术挑战？如何解决？"

---

## 📚 核心资源汇总

### Week 1-2: RAG

**必看**:
1. LangChain RAG Tutorial (2小时)
   - https://python.langchain.com/docs/tutorials/rag/
   
2. Chroma Quickstart (30分钟)
   - https://docs.trychroma.com/getting-started
   
3. Streamlit文档 (1小时)
   - https://docs.streamlit.io/get-started

**代码模板**:
- RAG基础模板（见Day 1-2）
- Streamlit界面模板（见Day 6-7）

---

### Week 3: Fine-tuning

**必看**:
1. HuggingFace Quickstart (1小时)
   - https://huggingface.co/docs/transformers/quicktour
   
2. Fine-tuning Guide (2小时)
   - https://huggingface.co/docs/transformers/training
   
3. Financial PhraseBank数据集
   - https://huggingface.co/datasets/financial_phrasebank

**代码模板**:
- Fine-tuning完整脚本（见Day 3-5）
- FastAPI部署（见Day 6-7）

---

### Week 4-5: Agent

**必看**:
1. LangChain Quickstart (1小时)
   - https://python.langchain.com/docs/get-started/quickstart
   
2. LangGraph Tutorial (3小时)
   - https://langchain-ai.github.io/langgraph/tutorials/introduction/
   
3. Tool Calling Guide (1小时)
   - https://python.langchain.com/docs/how_to/custom_tools/

**代码模板**:
- LangChain基础（见Day 1）
- LangGraph工作流（见Day 2-3）
- Multi-agent系统（见Day 4-7）

---

## 🔧 快速参考

### 常用代码模板

#### RAG查询模板

```python
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_community.vectorstores import Chroma
from langchain.chains import RetrievalQA

# 快速RAG
def quick_rag(docs_path):
    # 加载 → 切分 → 嵌入 → 检索
    loader = DirectoryLoader(docs_path)
    docs = loader.load()
    
    splitter = RecursiveCharacterTextSplitter(chunk_size=1000)
    chunks = splitter.split_documents(docs)
    
    vectorstore = Chroma.from_documents(chunks, OpenAIEmbeddings())
    
    qa = RetrievalQA.from_chain_type(
        llm=ChatOpenAI(),
        retriever=vectorstore.as_retriever()
    )
    
    return qa
```

#### Agent工作流模板

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict

class State(TypedDict):
    input: str
    output: str

def process(state: State) -> State:
    # 你的逻辑
    return {"output": f"Processed: {state['input']}"}

# 构建工作流
workflow = StateGraph(State)
workflow.add_node("process", process)
workflow.set_entry_point("process")
workflow.add_edge("process", END)

app = workflow.compile()
result = app.invoke({"input": "test", "output": ""})
```

#### Tool定义模板

```python
from langchain.tools import tool

@tool
def your_tool(param: str) -> str:
    """Clear description of what this tool does."""
    # 你的实现
    result = do_something(param)
    return str(result)
```

---

## 🐛 常见问题速查

### RAG相关

**问题**: 检索不准确
```python
# 解决方案：
1. 调整chunk_size（试500, 1000, 1500）
2. 使用Hybrid search（语义+关键词）
3. 添加Re-ranking
```

**问题**: 回答不相关
```python
# 解决方案：
1. 优化Prompt模板
2. 增加retrieved documents数量（k=3→k=5）
3. 改进chunk overlap
```

### Agent相关

**问题**: Agent选错工具
```python
# 解决方案：
1. 优化tool description（清晰描述何时使用）
2. 添加few-shot examples
3. 使用structured output
```

**问题**: 工作流卡住
```python
# 解决方案：
1. 添加verbose=True查看日志
2. 检查条件判断逻辑
3. 添加超时机制
```

### Fine-tuning相关

**问题**: 训练慢
```python
# 解决方案：
1. 减小batch_size
2. 使用更小的模型（distilbert）
3. 减少训练数据量
```

**问题**: 效果差
```python
# 解决方案：
1. 增加训练epochs
2. 检查数据质量
3. 尝试不同的学习率
```

---

## ✅ 每周验收清单

### Week 1-2验收
- [ ] RAG系统能正确回答问题
- [ ] 支持PDF/TXT文档
- [ ] 有Streamlit界面
- [ ] 能展示给别人看

### Week 3验收
- [ ] Fine-tuned模型完成
- [ ] 准确率报告
- [ ] 部署为API
- [ ] 能调用测试

### Week 4-5验收
- [ ] Multi-agent系统运行
- [ ] 至少3个agents协作
- [ ] 工作流清晰可见
- [ ] 输出质量好

### Week 6验收
- [ ] 前后端集成完成
- [ ] 完整功能演示
- [ ] README文档完善
- [ ] Demo视频录制
- [ ] 代码上传GitHub

---

## 🎓 学习节奏

### 每天时间安排（2-3小时）

```
7:00-7:30 (30分钟)
└─ 看教程/文档

7:30-9:00 (1.5小时)
└─ 写代码实践

9:00-9:30 (30分钟)
└─ 测试调试

9:30-10:00 (30分钟)
└─ 写日志，提交代码
```

### 周末冲刺（4-6小时）

```
上午: 完成本周项目核心功能
下午: 优化代码，添加功能
晚上: 写README，录demo
```

---

## 🚀 立刻开始（今晚行动清单）

### 第1小时: 环境搭建

```bash
# 创建项目
mkdir ai-stock-research
cd ai-stock-research

# 虚拟环境
python -m venv venv
source venv/bin/activate

# 安装依赖
pip install langchain langchain-openai langgraph chromadb streamlit

# 测试
python -c "
from langchain_openai import ChatOpenAI
print('✅ 环境准备完成!')
"

# 创建GitHub repo
git init
echo '# AI Stock Research System' > README.md
git add .
git commit -m "Initial commit"
```

### 第2小时: 第一个RAG原型

```bash
# 复制"Day 1-2: 第一个RAG原型"的代码
# 保存为simple_rag.py
# 运行测试
python simple_rag.py
```

### 今晚结束时你已经有:
- ✅ 能跑的RAG系统
- ✅ GitHub项目
- ✅ 第一个可demo的原型

---

## 📊 进度追踪

### 学习日志模板

```markdown
# AI学习日志

## Week 1: RAG系统

### Day 1 - 2025-01-05
**用时**: 2小时
**完成**:
- [x] 环境搭建
- [x] 第一个RAG原型
- [x] 测试查询功能

**问题**:
- Chroma初始化报错 → 已解决（版本问题）

**明天计划**:
- 添加PDF支持
- 优化chunk_size
```

---

## 💡 成功要素

### 关键原则

```python
✅ 快速迭代
   - 先跑通，再优化
   - MVP思维

✅ 复制粘贴
   - 用现成模板
   - 不重复造轮子

✅ 立刻实践
   - 看到代码立刻运行
   - 修改参数观察效果

✅ 及时求助
   - 卡住1小时就问
   - 不要死磕
```

### 避免的坑

```
❌ 追求完美（够用就行）
❌ 深究原理（工程优先）
❌ 收藏不练（立刻动手）
❌ 孤军奋战（及时求助）
```

---

## 🎯 6周后你将拥有

### 技能清单
- ✅ RAG系统开发
- ✅ 模型Fine-tuning
- ✅ Multi-agent编排
- ✅ LangChain/LangGraph熟练使用
- ✅ AI应用全栈开发

### 项目作品
- ✅ RAG知识库系统
- ✅ Fine-tuned金融模型
- ✅ Multi-agent股票分析系统
- ✅ 完整的GitHub portfolio

### 面试准备
- ✅ 可demo的实战项目
- ✅ 技术架构讲解能力
- ✅ 问题解决经验
- ✅ AI工程化理解

---

## 📞 获取帮助

**遇到问题**:
1. Google搜索错误信息
2. 查看官方文档
3. 问我（提供代码和错误信息）

**提问模板**:
```
我在[做什么]时遇到[什么错误]

代码:
[minimal reproducible example]

错误信息:
[完整error message]

我的理解:
[你的分析]

我尝试了:
[你试过的方法]

请问如何解决?
```

---

## 🎉 开始吧！

**现在立刻执行**:

```bash
# 5分钟后开始第一行代码
mkdir ai-stock-research
cd ai-stock-research
python -m venv venv
source venv/bin/activate
pip install langchain langchain-openai langgraph chromadb

# 开始Day 1的学习！
```

**6周后见证你的AI项目！** 🚀

---

*文档版本: v1.0*  
*创建日期: 2025-01-05*  
*维护者: Jason*
